using UnityEngine;

[CreateAssetMenu(fileName = "Enemies", menuName = "Enemies/Shambler")]
public class ShamblerData : EnemyData 
{
    // Shambler most basic unit...
    // Only needs default stats with no special overrides
}
